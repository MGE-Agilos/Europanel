{
  "name": "europanel",
  "version": "1.0.0",
  "private": true,
  "description": "WBP BREF data collection platform",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\" \"tests/**/*.test.mjs\""
  }
}
