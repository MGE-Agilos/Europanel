// ═══════════════════════════════════════════════════════════════════
// EuroPanel — Page 1 — general_info (5 tables)
// Généré par tools/gen_qlik_load_script.mjs — ne pas éditer à la main :
// toute correction doit passer par docs/fields.js puis régénérer.
// submission_id (et, pour les tables enfants, la colonne *_id de leur
// table parente) est la seule clé nue. Tout le reste est préfixé par le
// nom de sa table pour éviter une association accidentelle avec une autre
// section du questionnaire.
// ═══════════════════════════════════════════════════════════════════
LIB CONNECT TO 'Europanel:Europanel';

// Plafond impose par PostgREST, pas un choix de confort : demander
// davantage ne rend pas davantage, et rien n'avertit du plafonnement.
LET vEpPageSize = 1000;
// general_info
// Coquille vide, puis une page par tour de boucle. PostgREST ne rend
// JAMAIS plus de 1000 lignes par requête et ne signale pas qu'il tronque :
// un SELECT nu sur une table plus longue chargerait 1000 lignes et
// rechargerait « avec succès ». La boucle s'arrête sur la première page
// incomplète ; une page vide est sans effet (vérifié dans l'application).
[general_info]:
LOAD * INLINE [
submission_id, general_info_plant_name, general_info_production_started, general_info_location_city, general_info_location_country, general_info_company, general_info_ref_year, general_info_comments, general_info_updated_at
];

LET vEpOffset = 0;
DO
	RestConnectorMasterTable:
	SQL SELECT
		"submission_id",
		"plant_name",
		"production_started",
		"location_city",
		"location_country",
		"company",
		"ref_year",
		"comments",
		"updated_at"
	FROM JSON (wrap on) "root"
	WITH CONNECTION (Url "https://vxwmhsgxoomcbiukeinp.supabase.co/rest/v1/general_info?limit=$(vEpPageSize)&offset=$(vEpOffset)");

	LET vEpRows = NoOfRows('RestConnectorMasterTable');

	CONCATENATE ([general_info])
	LOAD
		[submission_id],
		[plant_name] as [general_info_plant_name],
		[production_started] as [general_info_production_started],
		[location_city] as [general_info_location_city],
		[location_country] as [general_info_location_country],
		[company] as [general_info_company],
		[ref_year] as [general_info_ref_year],
		[comments] as [general_info_comments],
		[updated_at] as [general_info_updated_at]
	RESIDENT RestConnectorMasterTable;

	DROP TABLE RestConnectorMasterTable;
	LET vEpOffset = $(vEpOffset) + $(vEpPageSize);
LOOP WHILE vEpRows = $(vEpPageSize)

// plant_products
// Coquille vide, puis une page par tour de boucle. PostgREST ne rend
// JAMAIS plus de 1000 lignes par requête et ne signale pas qu'il tronque :
// un SELECT nu sur une table plus longue chargerait 1000 lignes et
// rechargerait « avec succès ». La boucle s'arrête sur la première page
// incomplète ; une page vide est sans effet (vérifié dans l'application).
[plant_products]:
LOAD * INLINE [
plant_products_id, submission_id, plant_products_idx, plant_products_type, plant_products_addinfo, plant_products_qty, plant_products_unit, plant_products_daily, plant_products_updated_at
];

LET vEpOffset = 0;
DO
	RestConnectorMasterTable2:
	SQL SELECT
		"id",
		"submission_id",
		"idx",
		"type",
		"addinfo",
		"qty",
		"unit",
		"daily",
		"updated_at"
	FROM JSON (wrap on) "root"
	WITH CONNECTION (Url "https://vxwmhsgxoomcbiukeinp.supabase.co/rest/v1/plant_products?limit=$(vEpPageSize)&offset=$(vEpOffset)");

	LET vEpRows = NoOfRows('RestConnectorMasterTable2');

	CONCATENATE ([plant_products])
	LOAD
		[id] as [plant_products_id],
		[submission_id],
		[idx] as [plant_products_idx],
		[type] as [plant_products_type],
		[addinfo] as [plant_products_addinfo],
		[qty] as [plant_products_qty],
		[unit] as [plant_products_unit],
		[daily] as [plant_products_daily],
		[updated_at] as [plant_products_updated_at]
	RESIDENT RestConnectorMasterTable2;

	DROP TABLE RestConnectorMasterTable2;
	LET vEpOffset = $(vEpOffset) + $(vEpPageSize);
LOOP WHILE vEpRows = $(vEpPageSize)

// site_activities
// Coquille vide, puis une page par tour de boucle. PostgREST ne rend
// JAMAIS plus de 1000 lignes par requête et ne signale pas qu'il tronque :
// un SELECT nu sur une table plus longue chargerait 1000 lignes et
// rechargerait « avec succès ». La boucle s'arrête sur la première page
// incomplète ; une page vide est sans effet (vérifié dans l'application).
[site_activities]:
LOAD * INLINE [
site_activities_id, submission_id, site_activities_code, site_activities_present, site_activities_ippc, site_activities_capacity, site_activities_updated_at, site_activities_ref_label, site_activities_ref_unit
];

LET vEpOffset = 0;
DO
	RestConnectorMasterTable3:
	SQL SELECT
		"id",
		"submission_id",
		"code",
		"list_code",
		"present",
		"ippc",
		"capacity",
		"updated_at"
	FROM JSON (wrap on) "root"
	WITH CONNECTION (Url "https://vxwmhsgxoomcbiukeinp.supabase.co/rest/v1/site_activities?limit=$(vEpPageSize)&offset=$(vEpOffset)");

	LET vEpRows = NoOfRows('RestConnectorMasterTable3');

	CONCATENATE ([site_activities])
	LOAD
		[id] as [site_activities_id],
		[submission_id],
		[code] as [site_activities_code],
		[present] as [site_activities_present],
		[ippc] as [site_activities_ippc],
		[capacity] as [site_activities_capacity],
		[updated_at] as [site_activities_updated_at],
		ApplyMap('RefLabelMap', [list_code] & '|' & [code], [code]) as [site_activities_ref_label],
		ApplyMap('RefUnitMap', [list_code] & '|' & [code], null()) as [site_activities_ref_unit]
	RESIDENT RestConnectorMasterTable3;

	DROP TABLE RestConnectorMasterTable3;
	LET vEpOffset = $(vEpOffset) + $(vEpPageSize);
LOOP WHILE vEpRows = $(vEpPageSize)

// site_activity_units
// Coquille vide, puis une page par tour de boucle. PostgREST ne rend
// JAMAIS plus de 1000 lignes par requête et ne signale pas qu'il tronque :
// un SELECT nu sur une table plus longue chargerait 1000 lignes et
// rechargerait « avec succès ». La boucle s'arrête sur la première page
// incomplète ; une page vide est sans effet (vérifié dans l'application).
[site_activity_units]:
LOAD * INLINE [
site_activity_units_id, submission_id, site_activity_units_code, site_activity_units_unit, site_activity_units_updated_at, site_activity_units_ref_label, site_activity_units_ref_unit
];

LET vEpOffset = 0;
DO
	RestConnectorMasterTable4:
	SQL SELECT
		"id",
		"submission_id",
		"code",
		"list_code",
		"unit",
		"updated_at"
	FROM JSON (wrap on) "root"
	WITH CONNECTION (Url "https://vxwmhsgxoomcbiukeinp.supabase.co/rest/v1/site_activity_units?limit=$(vEpPageSize)&offset=$(vEpOffset)");

	LET vEpRows = NoOfRows('RestConnectorMasterTable4');

	CONCATENATE ([site_activity_units])
	LOAD
		[id] as [site_activity_units_id],
		[submission_id],
		[code] as [site_activity_units_code],
		[unit] as [site_activity_units_unit],
		[updated_at] as [site_activity_units_updated_at],
		ApplyMap('RefLabelMap', [list_code] & '|' & [code], [code]) as [site_activity_units_ref_label],
		ApplyMap('RefUnitMap', [list_code] & '|' & [code], null()) as [site_activity_units_ref_unit]
	RESIDENT RestConnectorMasterTable4;

	DROP TABLE RestConnectorMasterTable4;
	LET vEpOffset = $(vEpOffset) + $(vEpPageSize);
LOOP WHILE vEpRows = $(vEpPageSize)

// site_activity_other_specify
// Coquille vide, puis une page par tour de boucle. PostgREST ne rend
// JAMAIS plus de 1000 lignes par requête et ne signale pas qu'il tronque :
// un SELECT nu sur une table plus longue chargerait 1000 lignes et
// rechargerait « avec succès ». La boucle s'arrête sur la première page
// incomplète ; une page vide est sans effet (vérifié dans l'application).
[site_activity_other_specify]:
LOAD * INLINE [
submission_id, site_activity_other_specify_act_other_specify_label, site_activity_other_specify_updated_at
];

LET vEpOffset = 0;
DO
	RestConnectorMasterTable5:
	SQL SELECT
		"submission_id",
		"act_other_specify_label",
		"updated_at"
	FROM JSON (wrap on) "root"
	WITH CONNECTION (Url "https://vxwmhsgxoomcbiukeinp.supabase.co/rest/v1/site_activity_other_specify?limit=$(vEpPageSize)&offset=$(vEpOffset)");

	LET vEpRows = NoOfRows('RestConnectorMasterTable5');

	CONCATENATE ([site_activity_other_specify])
	LOAD
		[submission_id],
		[act_other_specify_label] as [site_activity_other_specify_act_other_specify_label],
		[updated_at] as [site_activity_other_specify_updated_at]
	RESIDENT RestConnectorMasterTable5;

	DROP TABLE RestConnectorMasterTable5;
	LET vEpOffset = $(vEpOffset) + $(vEpPageSize);
LOOP WHILE vEpRows = $(vEpPageSize)

